# Ryvax reference application

This fixture intentionally stays dependency-light while exercising the framework's core lifecycle:

- SSR page rendering;
- nested dashboard surface;
- progressive-enhancement login form;
- JSON API route;
- SSE streaming route;
- deterministic route manifest and deployment build.

It is used by the conformance tests and benchmark tooling. It contains no production credentials.
