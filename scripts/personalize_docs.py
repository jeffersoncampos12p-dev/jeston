from pathlib import Path
import re

ROOT = Path('/home/ubuntu/jeston-repo/path/to/docs')

PAGES = {
'index': dict(eyebrow='Orientation', icon='rocket', lead='Choose a path through Jeston by the kind of system you are building, not by the order of the navigation.', outcome='A mental map from first install to production operations.', why='The framework is easier to evaluate when rendering, APIs, data, identity, and operations are treated as one request lifecycle.', diagram='''flowchart LR\n    idea[Product idea] --> route[Route and UI]\n    route --> data[Data and identity]\n    data --> signal[Signals and tests]\n    signal --> deploy[Deploy and operate]''', steps=[('Pick a slice','Start with installation, first app, or the SaaS starter according to your goal.'),('Learn the runtime','Read architecture, routing, and API contracts before adding providers.'),('Add platform weight','Choose SQL, auth, cache, jobs, storage, or agents only when the slice needs them.'),('Close the loop','Use benchmarks, release checks, and production guidance to prove the result.')], tabs=[('Build a UI','Follow React SSR, routing, and API routes.'),('Build a platform','Follow SQL, auth, cache, health, and deployment.'),('Build an agent','Follow AI agents, streaming, tools, and observability.')], rows=[('First question','What is the smallest valuable request?','Start with one route and one response.'),('Second question','What must remain portable?','Keep providers behind adapters.'),('Third question','How will success be proven?','Write a contract test and an operational signal.')], quote='A framework is a system boundary, not only a rendering shortcut.', note='Use the navigation as a map. Each section answers a different ownership question; do not read every page linearly.', accordions=[('Which page should I open first?','Installation for setup, first app for a vertical slice, architecture for the runtime model.'),('Where do AI workloads fit?','AI agents belongs after request context, streaming, health, and adapter behavior are understood.'),('How should I compare results?','Compare complete topologies, not isolated handler microbenchmarks.')], next=[('/start/installation','Start with installation','download'),('/core/architecture','Understand architecture','diagram-project'),('/operations/production','Prepare production','server-stack')]),
'start/installation': dict(eyebrow='Start · Installation', icon='download', lead='Make a new Jeston workspace reproducible before writing application code.', outcome='A clean install that another contributor can repeat on a supported Node runtime.', why='Most early framework friction comes from drift in Node versions, package managers, environment variables, and generated artifacts.', diagram='''flowchart LR\n    runtime[Node 20+] --> package[Package install]\n    package --> source[TypeScript source]\n    source --> check[Typecheck and tests]\n    check --> artifact[Build artifact]''', steps=[('Pin the runtime','Choose the Node version used by CI and local development.'),('Install the package','Keep the framework dependency explicit and inspect the lockfile.'),('Run the first checks','Verify typecheck, tests, build, and package contents before customization.'),('Record assumptions','Document environment variables, scripts, and the expected directory shape.')], tabs=[('npm','Use the published package and commit the lockfile.'),('CI','Run the same checks on every supported Node version.'),('Restricted network','Cache dependencies and fail with an actionable message.')], rows=[('Runtime','Node 20 or newer','Match the repository support matrix.'),('Verification','typecheck, test, build','Do this before adding providers.'),('Reproducibility','lockfile and scripts','A new clone should behave like the original.')], quote='A fast first request is less valuable than a repeatable first request.', note='Do not add Vite, Express, or a second application runtime to the Jeston setup.', accordions=[('What belongs in source control?','Source, lockfile, scripts, configuration examples, and documentation; never credentials.'),('What should fail early?','Unsupported Node versions, missing environment variables, and broken package exports.'),('What should the first commit prove?','That a clean checkout can install, typecheck, test, build, and start.')], next=[('/start/first-app','Build the first app','rocket'),('/reference/cli','Inspect the CLI','terminal'),('/operations/release-checks','Prepare release checks','circle-check')]),
'start/first-app': dict(eyebrow='Start · First application', icon='wand-magic-sparkles', lead='Connect a page, a server handler, and a real request contract into one small vertical slice.', outcome='A first application whose behavior can be inspected from browser to server response.', why='A vertical slice exposes integration mistakes earlier than a collection of disconnected components.', diagram='''sequenceDiagram\n    participant Browser\n    participant Jeston\n    participant Handler\n    Browser->>Jeston: Navigate or fetch\n    Jeston->>Handler: Build request context\n    Handler-->>Jeston: Typed response\n    Jeston-->>Browser: HTML or JSON''', steps=[('Create the route','Give the page or API endpoint a narrow responsibility.'),('Validate at the edge','Reject malformed input before domain work or persistence.'),('Return an explicit result','Choose status, headers, and body intentionally.'),('Exercise the failure path','Test bad input, missing data, and a cancelled request.')], tabs=[('Page','Use SSR for initial content and clear loading boundaries.'),('API','Keep method handling and response shapes explicit.'),('Test','Assert observable behavior rather than implementation details.')], rows=[('Page','Initial HTML','Useful without waiting for client work.'),('API','Status and JSON','Stable for callers and tests.'),('Failure','Safe error body','Actionable without leaking internals.')], quote='The first app should teach the request lifecycle, not hide it behind scaffolding.', note='Keep the first slice small enough to understand in one sitting; complexity belongs in later platform pages.', accordions=[('Why start vertically?','It proves routing, rendering, handler execution, and feedback together.'),('When should data be added?','After the route contract is stable; otherwise persistence obscures routing errors.'),('What is a useful smoke test?','Request the page, call the endpoint, inspect status, and verify the rendered result.')], next=[('/core/react-ssr','Understand SSR','server'),('/core/api-routes','Define API behavior','brackets-curly'),('/reference/http-contracts','Read HTTP contracts','globe')]),
'start/saas-starter': dict(eyebrow='Start · SaaS starter', icon='building', lead='Model a multi-user product as cooperating boundaries instead of one oversized route handler.', outcome='A SaaS foundation with tenant context, identity, billing seams, jobs, and operational ownership.', why='SaaS complexity is mostly coordination: who owns data, who may act, what can be retried, and how usage is measured.', diagram='''flowchart TB\n    user[User] --> tenant[Tenant context]\n    tenant --> policy[Authorization policy]\n    policy --> domain[Domain action]\n    domain --> billing[Usage and billing]\n    domain --> jobs[Durable jobs]''', steps=[('Define tenancy','Choose how tenant identity enters the request and how it is enforced.'),('Separate identity from authorization','A valid session does not automatically grant access to every tenant resource.'),('Move slow work to jobs','Email, exports, indexing, and model tasks need durable status.'),('Instrument usage','Record the signals required for limits, billing, and support.')], tabs=[('Single tenant','Optimize for clarity and simple local development.'),('Multi tenant','Make tenant scope explicit in every query and cache key.'),('Enterprise','Add auditability, stronger isolation, and operational runbooks.')], rows=[('Identity','Who is the actor?','Session and provider boundary.'),('Scope','Which tenant?','Request context and query predicates.'),('Usage','What was consumed?','Event or metering record.')], quote='A SaaS starter is a set of explicit ownership decisions, not a dashboard template.', note='Billing and authorization are consequential domains. Keep their policies in application code and test them independently.', accordions=[('Where should tenant scope live?','At the request boundary and again in persistence queries; never rely on a UI filter.'),('What needs idempotency?','Billing events, invitations, provisioning, and webhook-driven updates.'),('What becomes a job?','Work that may outlive the request or needs retry and durable status.')], next=[('/platform/auth','Secure identity','user-lock'),('/platform/sql','Scope data access','database'),('/operations/production','Operate the SaaS','server-stack')]),
'core/architecture': dict(eyebrow='Core · Architecture', icon='diagram-project', lead='See Jeston as a set of lifecycle contracts with replaceable platform edges.', outcome='A clear ownership map for compiler, router, request context, renderer, and adapters.', why='Architectural boundaries determine whether a heavy workload can be cancelled, measured, moved, or scaled.', diagram='''flowchart LR\n    compiler[Route compiler] --> manifest[Route manifest]\n    manifest --> runtime[HTTP runtime]\n    runtime --> context[Request context]\n    context --> renderer[React renderer]\n    context --> adapters[Platform adapters]''', steps=[('Map the lifecycle','Trace a request from route discovery to response completion.'),('Mark ownership','Separate framework lifecycle from application policy and provider mechanics.'),('Propagate context','Carry request ID, deadline, and AbortSignal into expensive work.'),('Test seams','Replace adapters in tests and exercise lifecycle failures directly.')], tabs=[('Framework','Own contracts, lifecycle, and transport semantics.'),('Application','Own domain rules, authorization, and composition.'),('Adapter','Own provider protocol, credentials, and resource cleanup.')], rows=[('Lifecycle','Framework','Stable and portable.'),('Policy','Application','Business-specific and testable.'),('Protocol','Adapter','Replaceable and provider-aware.')], quote='Portability is achieved by making ownership explicit, not by removing all abstractions.', note='Use architecture diagrams to review responsibility. If two layers both believe they own retries or shutdown, the boundary is incomplete.', accordions=[('Where does cancellation begin?','At the request boundary; every cancellable dependency should receive the signal.'),('What should not be global?','Tenant policy, credentials, domain state, and provider-specific retry rules.'),('How should scaling be evaluated?','Measure the full topology, including database, network, queue, and renderer.')], next=[('/core/routing','Trace route selection','route'),('/core/execution-and-streaming','Trace execution','stream'),('/reference/adapters','Design adapters','puzzle-piece')]),
'core/react-ssr': dict(eyebrow='Core · React SSR', icon='server', lead='Use server rendering to deliver useful HTML without turning hydration into an invisible second application.', outcome='A render path with explicit data ownership, hydration assumptions, and recovery behavior.', why='SSR performance is determined by data dependencies, serialization, cache behavior, and client takeover—not only render speed.', diagram='''sequenceDiagram\n    participant Request\n    participant Loader\n    participant React\n    participant Browser\n    Request->>Loader: Resolve server data\n    Loader->>React: Render with stable props\n    React-->>Browser: HTML and serialized state\n    Browser->>React: Hydrate interactive islands''', steps=[('Classify the data','Separate request-specific, cacheable, and client-only values.'),('Render the useful shell','Deliver meaningful structure before optional work.'),('Keep props serializable','Avoid hidden runtime objects crossing the server boundary.'),('Test hydration failure','Check mismatches, slow data, and server error rendering.')], tabs=[('Fast page','Prefer bounded data and a small initial tree.'),('Streaming page','Reveal independent sections progressively.'),('Interactive page','Keep client state narrow and deliberate.')], rows=[('Server','HTML and initial data','Optimize time to useful content.'),('Client','Events and local state','Avoid duplicating server ownership.'),('Failure','Fallback and status','Do not render a misleading success shell.')], quote='SSR is a contract between two runtimes: server output and browser continuation.', note='Do not call browser-only APIs during server rendering, and do not serialize secrets into the initial payload.', accordions=[('What belongs on the server?','Data access, secure decisions, and work that improves initial content.'),('What belongs in the browser?','Ephemeral interaction state and capabilities unavailable to the server.'),('What is a hydration smell?','A page that only works after client JavaScript runs or changes meaning during takeover.')], next=[('/core/execution-and-streaming','Add streaming','stream'),('/core/react-server-components','Evaluate RSC','atom'),('/platform/cache-jobs-storage','Coordinate cache','layer-group')]),
'core/routing': dict(eyebrow='Core · Routing', icon='route', lead='Treat route selection as a deterministic contract, especially when dynamic and catch-all paths coexist.', outcome='Predictable precedence, parameters, method boundaries, and 404 behavior.', why='Routing bugs are often silent: a request reaches a valid handler that was not the intended handler.', diagram='''flowchart TD\n    request[Incoming URL] --> exact[Exact route]\n    exact -->|miss| dynamic[Dynamic segment]\n    dynamic -->|miss| catchall[Catch-all route]\n    catchall -->|miss| notfound[404 contract]''', steps=[('List the route tree','Write static, dynamic, and catch-all paths before implementation.'),('Define precedence','Make the winner for overlapping paths obvious and tested.'),('Name parameters','Keep parameter names stable across pages and handlers.'),('Test the negative space','Exercise 404, malformed parameters, unsupported methods, and encoded paths.')], tabs=[('Static','Use for stable product surfaces and predictable caching.'),('Dynamic','Validate identifiers and scope them to authorization.'),('Catch-all','Use sparingly for controlled fallback or documentation surfaces.')], rows=[('Exact path','Highest certainty','Test before dynamic matches.'),('Parameter','Validated value','Never treat URL text as trusted domain input.'),('Fallback','Explicit 404','Avoid accidental broad handlers.')], quote='A route tree is an executable product map; ambiguity is a defect.', note='Keep route names and parameter semantics stable because clients, links, tests, and observability depend on them.', accordions=[('How should route precedence be documented?','Show the competing paths and the expected winner for representative URLs.'),('What is a safe dynamic segment?','A parsed, validated identifier with an authorization check.'),('When is catch-all appropriate?','When the fallback behavior is intentional, bounded, and tested.')], next=[('/core/api-routes','Add method contracts','brackets-curly'),('/reference/http-contracts','Define responses','globe'),('/core/configuration','Configure runtime','sliders')]),
'core/api-routes': dict(eyebrow='Core · API routes', icon='brackets-curly', lead='Make every endpoint a small, observable protocol rather than an arbitrary function behind a URL.', outcome='Stable methods, input validation, status codes, headers, errors, and cancellation.', why='A clear HTTP contract lets clients, tests, proxies, and operators reason about behavior independently.', diagram='''flowchart LR\n    request[Method and body] --> validate[Validate input]\n    validate --> authorize[Authorize]\n    authorize --> domain[Domain operation]\n    domain --> response[Status headers body]''', steps=[('Choose the method','Align semantics with read, create, replace, partial update, or delete behavior.'),('Validate before work','Return a stable 400 shape for malformed JSON or invalid fields.'),('Set status intentionally','Do not make clients infer success from a generic 200 response.'),('Bound the handler','Use deadlines, signals, body limits, and idempotency where repetition is possible.')], tabs=[('Read','Cache only when freshness and authorization semantics are clear.'),('Write','Make retries safe with idempotency or explicit non-retry policy.'),('Stream','Define event framing, disconnect behavior, and terminal state.')], rows=[('Malformed JSON','400','No domain side effect.'),('Unauthorized','401','Identity is absent or invalid.'),('Forbidden','403','Identity exists but policy denies.'),('Dependency failure','5xx','Safe public body and correlated internal log.')], quote='An API is a promise about failure as much as it is a promise about success.', note='Document headers and response bodies as carefully as input fields; clients depend on both.', accordions=[('What belongs in a 400?','Actionable input errors that the caller can correct without server changes.'),('When should a write be retried?','Only when the operation is idempotent or protected by an idempotency key.'),('What does HEAD require?','The same metadata semantics as GET without a response body.')], next=[('/reference/http-contracts','Read HTTP semantics','globe'),('/platform/security','Harden boundaries','shield-halved'),('/platform/health-observability','Expose signals','chart-line')]),
'core/configuration': dict(eyebrow='Core · Configuration', icon='sliders', lead='Make runtime choices visible, typed, and safe to override across development, CI, and production.', outcome='A configuration model that fails early and does not hide environment-specific behavior.', why='Configuration is executable policy: timeouts, limits, origins, credentials, and adapters change the system’s risk profile.', diagram='''flowchart TB\n    defaults[Safe defaults] --> parse[Parse environment]\n    parse --> validate[Validate shape and ranges]\n    validate --> runtime[Runtime configuration]\n    runtime --> logs[Redacted diagnostics]''', steps=[('Separate environments','Name which values are local conveniences and which are production requirements.'),('Parse once','Convert strings to typed values at startup, not deep inside handlers.'),('Validate ranges','Reject invalid timeouts, ports, limits, and origins before serving.'),('Redact diagnostics','Make configuration debuggable without logging secrets.')], tabs=[('Local','Fast feedback with safe development defaults.'),('CI','Deterministic values and no hidden credentials.'),('Production','Secret-manager injection, strict validation, and auditability.')], rows=[('Timeout','Typed duration','Bound expensive work.'),('Limit','Positive integer','Protect memory and concurrency.'),('Secret','Opaque value','Never include in logs or artifacts.')], quote='Configuration should explain why the process behaves differently, not conceal that difference.', note='Treat a missing production variable as a startup error, not as a request-time surprise.', accordions=[('What should be a default?','Only values that are safe across environments and easy to override.'),('What should be required?','Credentials, signing keys, and external endpoints needed by enabled capabilities.'),('How should configuration be tested?','Use fixtures for valid, missing, malformed, and out-of-range values.')], next=[('/core/architecture','Understand ownership','diagram-project'),('/platform/security','Protect secrets','shield-halved'),('/operations/deployment','Inject at deploy time','cloud-arrow-up')]),
'core/execution-and-streaming': dict(eyebrow='Core · Execution and streaming', icon='stream', lead='Keep long work bounded while still delivering useful progress to the client.', outcome='Deadlines, cancellation, backpressure, SSE framing, and terminal states that behave under disconnects.', why='Streaming increases responsiveness but also keeps resources open longer; its lifecycle must be more explicit than a normal response.', diagram='''sequenceDiagram\n    participant Client\n    participant Handler\n    participant Work\n    Client->>Handler: Start stream\n    Handler->>Work: Pass signal and deadline\n    Work-->>Client: Progress event\n    Client-->>Handler: Disconnect\n    Handler->>Work: Abort and release''', steps=[('Set a budget','Give the overall request and each dependency a deadline.'),('Define event shape','Choose event names, payloads, heartbeats, and terminal messages.'),('Propagate disconnect','Abort work when the client leaves or the stream is no longer useful.'),('Measure open work','Track active streams, duration, bytes, and cancellation reasons.')], tabs=[('SSE','Use explicit event framing and a terminal state.'),('Batch','Prefer bounded chunks when clients do not need immediacy.'),('Queue','Move work beyond request lifetime into a durable job.')], rows=[('Backpressure','Bound buffers','Never let a slow client grow memory without limit.'),('Disconnect','Abort signal','Stop model, database, and network work.'),('Terminal','Done or error event','Let clients close cleanly.')], quote='A stream is a resource lease, not an infinite response.', note='Do not keep a request open merely to hide a job. Persist status when work must survive the connection.', accordions=[('What is a heartbeat for?','It keeps intermediaries aware of a live connection; it is not a substitute for progress.'),('When should work move to a queue?','When it must survive disconnects, restart, or a request deadline.'),('How do you test streaming?','Test ordering, framing, slow consumers, cancellation, and terminal events.')], next=[('/operations/jobs-and-shutdown','Drain long work','power-off'),('/platform/ai-agents','Stream agent work','brain'),('/platform/health-observability','Measure streams','chart-line')]),
'core/react-server-components': dict(eyebrow='Core · React Server Components', icon='atom', lead='Treat RSC as a boundary to evaluate deliberately, not a label to apply to every component.', outcome='A clear decision about server/client ownership, serialization, caching, and maturity risk.', why='RSC changes where code executes and how data crosses the boundary; it affects tooling, debugging, and deployment assumptions.', diagram='''flowchart LR\n    server[Server component] --> payload[Serializable payload]\n    payload --> client[Client component]\n    client --> action[Browser interaction]\n    action --> server''', steps=[('Classify execution','Mark data access, secrets, browser APIs, and event handlers.'),('Define serialization','Keep the crossing payload explicit and stable.'),('Choose cache ownership','Decide whether freshness belongs to request, server, or client.'),('Record maturity','Separate supported behavior from an experimental or roadmap track.')], tabs=[('Server boundary','Use for secure data access and non-interactive composition.'),('Client boundary','Use for browser state, events, and device capabilities.'),('Hybrid','Keep the crossing narrow and inspect the payload.')], rows=[('Server code','No browser APIs','Runs in the server runtime.'),('Client code','Interactive behavior','Ships to the browser.'),('Payload','Serializable data','Defines the crossing contract.')], quote='A rendering model is mature when its boundaries are predictable under failure, not only when its demo is fast.', note='Document the current support envelope honestly; compatibility is part of the feature.', accordions=[('What should not cross?','Secrets, open handles, provider clients, and non-serializable runtime state.'),('What is a cache question?','Who owns freshness, invalidation, and authorization for the rendered data?'),('What should be measured?','Payload size, server time, hydration work, and failure recovery.')], next=[('/core/react-ssr','Compare SSR behavior','server'),('/core/architecture','Review runtime layers','diagram-project'),('/reference/compatibility','Check support envelope','scale-balanced')]),
'platform/sql': dict(eyebrow='Platform · SQL', icon='database', lead='Make persistence predictable under concurrency, retries, migrations, and partial failure.', outcome='A query boundary with transaction ownership, pool limits, migration discipline, and safe errors.', why='Database behavior dominates many heavy workloads; correctness and saturation matter before query micro-optimizations.', diagram='''flowchart LR\n    handler[Handler] --> transaction[Transaction boundary]\n    transaction --> pool[Connection pool]\n    pool --> database[(MariaDB or MySQL)]\n    database --> telemetry[Query timing and errors]''', steps=[('Define the unit of work','Decide which writes commit together and which can be asynchronous.'),('Parameterize queries','Keep values separate from SQL text and validate sort or filter choices.'),('Bound the pool','Match concurrency to database capacity, not to request volume.'),('Plan migrations','Use expand, migrate, contract when old and new code overlap.')], tabs=[('Read path','Index for real predicates and measure result size.'),('Write path','Use transactions and idempotency for repeated delivery.'),('Migration','Keep compatibility during rolling deploys and backfills.')], rows=[('Pool','Finite connections','Protect the database from application concurrency.'),('Transaction','Atomic unit','Make partial writes impossible where required.'),('Migration','Compatible step','Allow old and new versions to coexist.')], quote='A database abstraction is successful when it makes unsafe concurrency difficult to express.', note='Never put unbounded result sets, dynamic identifiers, or secret-bearing SQL errors into a public response.', accordions=[('What belongs in a transaction?','State changes that must share one commit decision.'),('What should be retried?','Transient, idempotent operations with clear transaction semantics.'),('How should pool pressure appear?','As a metric and alert before requests fail en masse.')], next=[('/platform/cache-jobs-storage','Add cache and jobs','layer-group'),('/operations/migrations','Evolve schemas','arrows-rotate'),('/operations/benchmark','Measure persistence','gauge-high')]),
'platform/auth': dict(eyebrow='Platform · Authentication', icon='user-lock', lead='Separate proving who someone is from deciding what that person may do.', outcome='Signed sessions, secure cookies, authorization checks, rotation, and revocation behavior.', why='Authentication bugs become data-boundary bugs when identity and tenant scope are implicit.', diagram='''flowchart LR\n    credentials[Credentials] --> identity[Identity provider]\n    identity --> session[Signed session]\n    session --> request[Request context]\n    request --> policy[Authorization policy]\n    policy --> resource[Protected resource]''', steps=[('Choose the identity source','Define credentials, external provider, or service identity.'),('Issue a bounded session','Set expiry, cookie flags, rotation, and revocation expectations.'),('Authorize every resource','Check ownership or capability at the domain boundary.'),('Exercise recovery','Test expiry, logout, key rotation, replay, and missing identity.')], tabs=[('Browser','HttpOnly, Secure, SameSite, and CSRF posture.'),('API','Bearer or service credentials with explicit scopes.'),('Internal job','Carry actor and tenant context without pretending it is a browser session.')], rows=[('Authentication','401','Identity absent or invalid.'),('Authorization','403','Identity present but policy denies.'),('Revocation','Immediate or bounded','Document the security tradeoff.')], quote='A session identifies an actor; it never grants permission by itself.', note='Do not log raw tokens, credentials, or complete session payloads.', accordions=[('Where should authorization run?','Near the resource access, not only in the route or UI.'),('When rotate keys?','Before expiry or compromise forces an emergency change; support overlap where necessary.'),('What is logout?','A client action plus server-side invalidation strategy when immediate revocation is required.')], next=[('/platform/security','Harden identity','shield-halved'),('/start/saas-starter','Apply tenancy','building'),('/reference/http-contracts','Return auth errors','globe')]),
'platform/security': dict(eyebrow='Platform · Security', icon='shield-halved', lead='Turn security from a checklist into a set of enforced boundaries with observable failure.', outcome='Defensive defaults for input, secrets, cookies, authorization, limits, and incident response.', why='Security posture is the combined effect of every route, adapter, default, and operational shortcut.', diagram='''flowchart TB\n    untrusted[Untrusted input] --> validate[Validate and normalize]\n    validate --> authorize[Authorize] --> execute[Bounded execution]\n    execute --> redact[Safe output and telemetry]''', steps=[('List trust zones','Mark browser, network, process, database, provider, and operator boundaries.'),('Fail closed','Require explicit authorization and reject ambiguous input.'),('Limit resources','Bound bodies, concurrency, streams, cache entries, and retries.'),('Prepare response','Keep secrets out of logs and define an incident investigation path.')], tabs=[('Application','Validation, authorization, output encoding, and error policy.'),('Infrastructure','TLS, network controls, secret manager, and runtime isolation.'),('Operations','Alerts, access review, rotation, and incident runbooks.')], rows=[('Input','Untrusted','Parse, validate, constrain.'),('Secret','Sensitive','Inject, redact, rotate.'),('Error','Observable','Correlate without disclosure.')], quote='Secure defaults are useful because the unsafe path should require deliberate action.', note='Threat modeling is most effective when attached to a concrete request, data flow, or deployment change.', accordions=[('What is a dangerous default?','A permissive origin, unlimited body, verbose error, or implicit authorization.'),('What should be redacted?','Credentials, tokens, session data, personal data, and provider secrets.'),('What does readiness mean?','The service can safely receive traffic, not merely that the process is alive.')], next=[('/platform/auth','Secure sessions','user-lock'),('/reference/http-contracts','Harden HTTP','globe'),('/operations/production','Run securely','server-stack')]),
'platform/cache-jobs-storage': dict(eyebrow='Platform · Cache, jobs, and storage', icon='layer-group', lead='Coordinate ephemeral speed, durable work, and object lifecycle without confusing their guarantees.', outcome='Explicit freshness, retry, durability, ownership, and cleanup semantics.', why='Cache, queues, and storage solve different problems; combining their names does not combine their guarantees.', diagram='''flowchart LR\n    request[Request] --> cache{Fresh cache?}\n    cache -->|miss| source[(Source of truth)]\n    source --> cache\n    request --> job[Durable job]\n    job --> object[Object storage]''', steps=[('Name the guarantee','State whether data is fresh, eventually consistent, durable, or disposable.'),('Choose the key','Include tenant, version, locale, and authorization dimensions where needed.'),('Make jobs repeatable','Use idempotency, retry classification, and durable status.'),('Close the lifecycle','Expire cache, delete objects, and drain workers intentionally.')], tabs=[('Cache','Optimize reads with documented staleness.'),('Job','Move work past request lifetime with status and retries.'),('Storage','Track content type, ownership, retention, and deletion.')], rows=[('Cache','Fast and disposable','Never the only source for critical state.'),('Job','Durable intent','Must survive a request ending.'),('Object','Large payload','Needs access and retention policy.')], quote='The right storage primitive is defined by its failure and lifecycle semantics.', note='A stale cache, a duplicate job, and an orphaned object are different operational problems.', accordions=[('When stale-while-revalidate helps?','When a bounded amount of staleness is acceptable and the refresh can be deduplicated.'),('What makes a job safe?','Idempotent effects, explicit retry classes, and durable status.'),('Who owns deletion?','The domain or retention policy must name the cleanup owner.')], next=[('/platform/sql','Choose a source of truth','database'),('/operations/jobs-and-shutdown','Drain workers','power-off'),('/platform/health-observability','Expose queue health','chart-line')]),
'platform/health-observability': dict(eyebrow='Platform · Health and observability', icon='chart-line', lead='Give operators enough signal to distinguish a healthy process from a useful service.', outcome='Liveness, readiness, structured logs, metrics, traces, and alertable dependency state.', why='Without health semantics, an overloaded or disconnected service can look alive while causing user-visible failure.', diagram='''flowchart LR\n    process[Liveness] --> ready[Readiness]\n    ready --> traffic[Traffic]\n    traffic --> logs[Logs]\n    traffic --> metrics[Metrics]\n    traffic --> traces[Traces]''', steps=[('Define liveness','Answer whether the process can continue or should be restarted.'),('Define readiness','Answer whether required dependencies and capacity permit traffic.'),('Correlate work','Carry request IDs, job IDs, provider labels, and tenant-safe context.'),('Alert on symptoms','Use latency, errors, saturation, and freshness rather than log volume alone.')], tabs=[('Logs','Explain one event with structured fields.'),('Metrics','Show rates, errors, duration, and saturation over time.'),('Traces','Follow a request across database, network, model, and queue boundaries.')], rows=[('Liveness','Process state','Should restart if broken.'),('Readiness','Dependency state','Should stop receiving traffic if unsafe.'),('Saturation','Capacity state','Predicts failure before error rate rises.')], quote='Observability is the interface between a running system and the person responsible for it.', note='Never put raw personal data or secrets into a correlation field merely because it makes debugging convenient.', accordions=[('What belongs in readiness?','Dependencies required for safe traffic, not every optional integration.'),('Which percentile matters?','p95 and p99 expose tail behavior hidden by averages.'),('What makes an alert useful?','A threshold, an owner, a runbook, and a reason the user is affected.')], next=[('/operations/production','Build runbooks','server-stack'),('/operations/benchmark','Measure saturation','gauge-high'),('/core/execution-and-streaming','Observe streams','stream')]),
'platform/ai-agents': dict(eyebrow='Platform · AI agents', icon='brain', lead='Build agent execution as a controlled workflow with tools, budgets, traces, and evaluation—not as an unbounded chat loop.', outcome='Model adapters, permissioned tools, durable state, streaming, and measurable quality.', why='Agent workloads combine nondeterministic model output with deterministic application side effects.', diagram='''flowchart LR\n    goal[User goal] --> plan[Model plan]\n    plan --> policy[Tool policy]\n    policy --> tool[Tool execution]\n    tool --> evidence[Result and trace]\n    evidence --> plan\n    plan --> done[Bounded completion]''', steps=[('Define the capability','State what the agent may decide and what it may never do.'),('Permission every tool','Validate arguments, identity, tenant scope, and side effects.'),('Bound the loop','Set token, time, tool-call, and spend budgets.'),('Evaluate behavior','Test groundedness, tool correctness, refusal, latency, and cost.')], tabs=[('Interactive','Stream plan and progress while keeping side effects explicit.'),('Background','Persist intent and status so work survives disconnects.'),('Training','Version datasets, prompts, evaluators, and model settings.')], rows=[('Model','Probabilistic','Treat output as untrusted input.'),('Tool','Deterministic effect','Validate and authorize every call.'),('Evaluation','Quality signal','Keep a regression set and trace failures.')], quote='An agent should be powerful inside a policy envelope, not powerful because the envelope is absent.', note='Never let model text directly become a database query, shell command, permission decision, or external message.', accordions=[('What belongs in memory?','Only information with a retention, tenant, privacy, and deletion policy.'),('What should be traced?','Prompt version, model, tool calls, latency, token usage, outcomes, and safe error categories.'),('When use a queue?','When agent work must survive disconnects or may exceed request deadlines.')], next=[('/core/execution-and-streaming','Stream agent work','stream'),('/platform/security','Protect tools','shield-halved'),('/operations/benchmark','Benchmark agents','gauge-high')]),
'reference/types': dict(eyebrow='Reference · Types', icon='code', lead='Use types to make lifecycle assumptions visible without pretending compile-time types validate network input.', outcome='A readable contract for contexts, handlers, responses, errors, and adapter capabilities.', why='Types reduce accidental mismatch between layers, but boundary validation remains a runtime responsibility.', diagram='''flowchart LR\n    wire[Network data] --> parse[Runtime validation]\n    parse --> typed[Typed domain input]\n    typed --> handler[Typed handler]\n    handler --> result[Typed result]''', steps=[('Type the context','Expose request ID, signal, deadline, params, and body deliberately.'),('Separate input and output','Do not reuse a domain type as an unvalidated network schema.'),('Model errors','Give callers stable categories and operators useful correlation.'),('Keep capabilities narrow','Adapters should implement only the operations they promise.')], tabs=[('Boundary types','Pair static types with runtime parsing.'),('Domain types','Represent invariants after validation.'),('Adapter types','Describe capability and lifecycle, not vendor internals.')], rows=[('Compile time','Developer feedback','Catches shape mismatch.'),('Runtime','Boundary safety','Catches hostile or malformed input.'),('Result','Caller contract','Makes success and failure explicit.')], quote='A type is a promise made by code; validation is the evidence that the outside world kept it.', note='Avoid broad `any` escape hatches at request and adapter boundaries.', accordions=[('Why not trust TypeScript at runtime?','Types are erased and cannot inspect untrusted JSON or headers.'),('What belongs in a result type?','Success data, stable error category, and correlation metadata when useful.'),('How should optional fields behave?','Define whether omission means unknown, default, or explicit absence.')], next=[('/reference/http-contracts','Pair types with HTTP','globe'),('/reference/adapters','Type capabilities','puzzle-piece'),('/core/api-routes','Apply handlers','brackets-curly')]),
'reference/cli': dict(eyebrow='Reference · CLI', icon='terminal', lead='Use the CLI as a shared vocabulary for creation, verification, local operations, and release work.', outcome='Commands that are discoverable, scriptable, and consistent with CI.', why='A CLI becomes infrastructure when teams use it in automation; ambiguous output and hidden side effects then become operational debt.', diagram='''flowchart LR\n    command[Command] --> parse[Parse arguments]\n    parse --> validate[Validate intent]\n    validate --> action[Perform action]\n    action --> output[Stable output and exit code]''', steps=[('List the command contract','Define arguments, defaults, files touched, and exit codes.'),('Keep dry runs safe','Preview migrations, releases, or destructive work where possible.'),('Make output scriptable','Use stable text or structured output for automation.'),('Run CI parity','Ensure local checks invoke the same underlying commands as CI.')], tabs=[('Explore','Use help and inspection commands without mutation.'),('Develop','Generate or verify local artifacts.'),('Release','Run checks, package inspection, and publication gates.')], rows=[('Exit code','Automation signal','Zero means the contract passed.'),('Output','Human and machine readable','Do not require screen scraping.'),('Mutation','Explicit','Show what will change before acting.')], quote='A good command leaves less ambiguity than the manual sequence it replaces.', note='Document whether a command is read-only, local-mutating, or externally consequential.', accordions=[('What should help include?','Purpose, examples, defaults, environment, output, and failure cases.'),('When add JSON output?','When another tool needs reliable fields instead of parsing prose.'),('What belongs in CI?','Checks that protect package integrity and runtime compatibility.')], next=[('/operations/release-checks','Run release gates','circle-check'),('/start/installation','Create a workspace','download'),('/reference/types','Understand outputs','code')]),
'reference/http-contracts': dict(eyebrow='Reference · HTTP contracts', icon='globe', lead='Document the wire behavior that clients and intermediaries actually observe.', outcome='Predictable methods, status codes, headers, body rules, OPTIONS, HEAD, and malformed input responses.', why='HTTP details are compatibility surface; a small inconsistency can break caches, SDKs, proxies, or browser behavior.', diagram='''flowchart LR\n    method[Method] --> route[Route match]\n    route --> allow[Allowed methods]\n    allow --> body[Parse body]\n    body --> status[Status and headers]\n    status --> representation[Representation]''', steps=[('Define method semantics','State whether the operation is safe, idempotent, cacheable, or streaming.'),('Define errors','Use stable status and body shapes for client, auth, dependency, and server failures.'),('Cover implicit methods','Verify OPTIONS and Allow; keep HEAD bodyless while preserving metadata.'),('Test intermediaries','Exercise cache headers, content type, length, and connection behavior.')], tabs=[('JSON API','Content type, malformed body, and error envelope.'),('Browser page','HTML, redirects, cookies, and cache behavior.'),('Stream','Event framing, heartbeat, disconnect, and terminal semantics.')], rows=[('OPTIONS','Capability discovery','Return allowed methods.'),('HEAD','Metadata without body','Mirror GET headers where appropriate.'),('400','Malformed request','Reject before side effects.')], quote='HTTP correctness is part of the framework API, even when the application code is only a few lines.', note='Do not make clients reverse-engineer status codes from successful examples alone.', accordions=[('Why document Allow?','Clients and tooling use it to discover method support.'),('What makes an error stable?','Status, content type, category, and safe fields remain predictable.'),('When set cache headers?','Only when freshness, authorization, and invalidation are understood.')], next=[('/core/api-routes','Implement contracts','brackets-curly'),('/core/routing','Resolve paths','route'),('/platform/security','Secure responses','shield-halved')]),
'reference/adapters': dict(eyebrow='Reference · Adapters', icon='puzzle-piece', lead='Create provider seams that preserve framework lifecycle while isolating vendor protocol and credentials.', outcome='Adapters with narrow capabilities, explicit cleanup, and replaceable test doubles.', why='An adapter is valuable when it makes provider choice reversible without weakening the application contract.', diagram='''flowchart LR\n    contract[Application capability] --> adapter[Adapter] --> provider[Vendor API]\n    adapter --> cleanup[Cleanup and timeout]\n    adapter --> metrics[Provider metrics]''', steps=[('Name the capability','Describe what the application needs, not what one vendor happens to call it.'),('Define lifecycle','Specify initialization, timeout, cancellation, close, and health behavior.'),('Map errors','Translate provider failures into stable application categories.'),('Prove replacement','Use a fake or second implementation to test the seam.')], tabs=[('Database','Queries, transactions, pool, and migration ownership.'),('Model','Generation, streaming, usage, and safety policy.'),('Storage','Upload, read, retention, authorization, and deletion.')], rows=[('Capability','Small interface','Avoid vendor-shaped application code.'),('Lifecycle','Open and close','Prevent leaked handles.'),('Failure','Translated category','Keep provider detail observable but not contagious.')], quote='The best adapter is boring at the call site and precise at the boundary.', note='Do not hide domain authorization or business retries in an infrastructure adapter.', accordions=[('When is an abstraction premature?','Before a real second implementation or a clear replacement requirement exists.'),('What should a fake simulate?','Latency, cancellation, transient failure, permanent failure, and result shape.'),('Who owns credentials?','The adapter boundary or platform secret manager, never route code.')], next=[('/reference/integrations','Register integrations','plug'),('/platform/sql','Apply a database adapter','database'),('/core/architecture','Review ownership','diagram-project')]),
'reference/compatibility': dict(eyebrow='Reference · Compatibility', icon='scale-balanced', lead='Make support claims precise enough that teams can plan upgrades and deployments without guesswork.', outcome='A compatibility envelope for Node, React, browsers, package exports, and known limitations.', why='Compatibility is a product feature: confidence depends on what is supported, tested, experimental, or explicitly out of scope.', diagram='''flowchart TB\n    runtime[Node runtime] --> framework[Jeston package]\n    framework --> react[React behavior]\n    react --> browser[Browser target]\n    framework --> adapter[Provider adapter]''', steps=[('Declare the matrix','List supported Node versions, package managers, React versions, and deployment assumptions.'),('Test the matrix','Run typecheck, tests, build, and package smoke import on every supported runtime.'),('Label maturity','Separate stable, experimental, roadmap, and unsupported behavior.'),('Plan migration','Document breaking changes, deprecations, and rollback options.')], tabs=[('Supported','Covered by CI and release expectations.'),('Experimental','Usable with an explicit risk statement.'),('Unsupported','Do not infer behavior from a local success.')], rows=[('Runtime','Node 20, 22, 24','CI evidence required.'),('Package','Exports and types','Smoke import from a consumer.'),('Feature','Maturity label','Do not overpromise roadmap behavior.')], quote='A compatibility statement is useful only when it can be checked.', note='Pin the assumptions used by examples so documentation does not silently outrun the package.', accordions=[('What is a breaking change?','Any change that invalidates supported code, runtime, output, or operational expectations.'),('How should experimental APIs appear?','With a visible label, limitation, and migration expectation.'),('What should a release prove?','All matrix checks plus package contents and a consumer import.')], next=[('/operations/release-checks','Verify releases','circle-check'),('/reference/types','Read stable types','code'),('/core/react-server-components','Review RSC maturity','atom')]),
'reference/integrations': dict(eyebrow='Reference · Integrations', icon='plug', lead='Grow the ecosystem through discoverable capabilities, lifecycle hooks, and compatibility metadata.', outcome='Integrations that can be registered, searched, evaluated, upgraded, and removed safely.', why='An ecosystem scales through governance and quality signals, not by accumulating an uncurated list of providers.', diagram='''flowchart LR\n    package[Integration package] --> manifest[Capability manifest]\n    manifest --> registry[Registry]\n    registry --> app[Application selection]\n    app --> health[Health and lifecycle]''', steps=[('Describe the capability','State supported operations, runtime, limits, and provider requirements.'),('Register metadata','Make discovery useful without forcing every application to install every provider.'),('Define hooks','Specify startup, shutdown, health, and configuration lifecycle.'),('Publish compatibility','Version the integration contract and document support expectations.')], tabs=[('Catalog','Browse providers by capability and maturity.'),('Plugin','Load lifecycle behavior with explicit ownership.'),('Internal','Register organization-specific adapters with private metadata.')], rows=[('Metadata','What it does','Discoverability and comparison.'),('Lifecycle','When it runs','Safe startup and shutdown.'),('Maturity','How trusted','Adoption and risk signal.')], quote='An ecosystem becomes powerful when integration quality is legible.', note='Avoid claiming millions of integrations without a capability contract, testing policy, and maintenance owner.', accordions=[('What belongs in a registry?','Machine-readable capability, compatibility, ownership, and maturity metadata.'),('How should plugins fail?','Startup should fail clearly for required plugins and degrade safely for optional ones.'),('What makes a provider professional?','Docs, tests, security policy, versioning, and a responsible maintainer.')], next=[('/reference/adapters','Design the seam','puzzle-piece'),('/operations/ecosystem','Govern the ecosystem','sitemap'),('/core/configuration','Configure providers','sliders')]),
'operations/deployment': dict(eyebrow='Operations · Deployment', icon='cloud-arrow-up', lead='Move the same verified artifact through environments with explicit configuration and reversible topology.', outcome='A deployment sequence with probes, process ownership, secret injection, and rollback.', why='Deployment is a controlled change to runtime behavior, not simply a command that starts Node.', diagram='''flowchart LR\n    commit[Commit] --> ci[CI verification]\n    ci --> artifact[Build artifact]\n    artifact --> deploy[Deploy] --> probe[Readiness probe]\n    probe --> traffic[Traffic]''', steps=[('Build once','Create an artifact that does not change between verification and deployment.'),('Inject configuration','Use the environment or secret manager at runtime, not source control.'),('Probe before traffic','Check liveness, readiness, and critical dependency behavior.'),('Release gradually','Keep rollback, drain, and previous artifact access available.')], tabs=[('Single process','Simple topology with clear limits and restart behavior.'),('Multiple workers','Coordinate ports, signals, shared state, and connection pools.'),('Distributed','Add network, queue, storage, and deployment health contracts.')], rows=[('Artifact','Immutable','Same bits across environments.'),('Probe','Actionable','Traffic only when safe.'),('Rollback','Available','Recovery without improvisation.')], quote='A deployment is successful when the operator can explain both how it starts and how it stops.', note='Do not treat a green build as proof that secrets, probes, database migrations, and traffic policy are correct.', accordions=[('What should readiness check?','Dependencies required to serve the advertised workload.'),('When can rollback fail?','After irreversible migrations, destructive jobs, or incompatible state changes.'),('What should logs include?','Version, environment, process phase, and correlation metadata without secrets.')], next=[('/operations/production','Run the service','server-stack'),('/operations/migrations','Change schemas','arrows-rotate'),('/operations/release-checks','Verify artifacts','circle-check')]),
'operations/production': dict(eyebrow='Operations · Production', icon='server-stack', lead='Operate Jeston as a workload with capacity, dependencies, incidents, and recovery—not as a static deployment.', outcome='A production posture for scaling, alerting, security, cost, and incident response.', why='Heavy systems fail at boundaries: queues fill, pools saturate, providers slow down, and tail latency grows before averages move.', diagram='''flowchart LR\n    traffic[Traffic] --> app[Application workers]\n    app --> db[(Database)]\n    app --> queue[Queue]\n    app --> providers[External providers]\n    app --> telemetry[Telemetry and alerts]''', steps=[('Set the capacity model','Estimate concurrency, pool size, memory, stream count, and provider quotas.'),('Define service objectives','Choose latency, error, freshness, and availability signals that matter.'),('Write runbooks','Give each alert an owner, diagnosis path, mitigation, and escalation.'),('Exercise failure','Practice dependency loss, overload, deploy rollback, and shutdown.')], tabs=[('Normal day','Dashboards, releases, capacity, and cost review.'),('Degraded day','Rate limits, fallback, queue draining, and communication.'),('Incident','Contain, diagnose, recover, and record learning.')], rows=[('Saturation','Queue, pool, CPU, memory','Predicts user-visible failure.'),('Tail latency','p95 and p99','Shows the slow users.'),('Recovery','Time to restore','Measures operational readiness.')], quote='Production maturity is the ability to recover predictably, not the absence of incidents.', note='Optimize the bottleneck measured in the complete topology; do not optimize framework throughput in isolation.', accordions=[('What should scale first?','The bottleneck that limits the user-visible workload, often a dependency or pool.'),('What should be rate limited?','Expensive, tenant-amplifiable, or externally quota-bound work.'),('What belongs in an incident record?','Impact, timeline, signals, decisions, recovery, and prevention.')], next=[('/operations/benchmark','Measure load','gauge-high'),('/platform/health-observability','Build telemetry','chart-line'),('/operations/jobs-and-shutdown','Drain safely','power-off')]),
'operations/migrations': dict(eyebrow='Operations · Migrations', icon='arrows-rotate', lead='Change persisted state while old and new application versions may coexist.', outcome='Expand-migrate-contract plans, backfills, compatibility windows, and rollback boundaries.', why='A schema change is deployed across time; code, workers, replicas, and operators do not switch versions simultaneously.', diagram='''flowchart LR\n    expand[Add compatible shape] --> dual[Write or read both]\n    dual --> backfill[Backfill safely]\n    backfill --> switch[Switch consumers]\n    switch --> contract[Remove old shape]''', steps=[('Expand','Add nullable or additive structures that old code can ignore.'),('Migrate','Write new data and backfill in bounded, observable batches.'),('Switch','Move readers and writers after evidence, not after hope.'),('Contract','Remove old fields only when all consumers and rollback windows are closed.')], tabs=[('Additive','Columns, indexes, or tables that do not break old code.'),('Backfill','Chunked work with checkpoints and pause controls.'),('Destructive','Requires explicit approval, backup, and a recovery plan.')], rows=[('Compatibility','Old plus new','Supports rolling deployment.'),('Backfill','Bounded batches','Protects production load.'),('Rollback','State-aware','May require a forward fix after contraction.')], quote='The safest migration is a sequence of compatible states, not one clever statement.', note='Treat data deletion and irreversible transformations as high-impact operations with a separate recovery decision.', accordions=[('When dual writes help?','When readers and writers transition at different times and consistency can be reconciled.'),('How should backfills run?','With idempotent chunks, rate limits, checkpoints, and pause/resume.'),('What invalidates rollback?','Dropping data or changing semantics without retaining a reversible representation.')], next=[('/platform/sql','Review database boundaries','database'),('/operations/deployment','Coordinate rollout','cloud-arrow-up'),('/operations/jobs-and-shutdown','Control backfills','power-off')]),
'operations/benchmark': dict(eyebrow='Operations · Benchmark', icon='gauge-high', lead='Measure the complete workload honestly, including concurrency, dependencies, memory, and tail behavior.', outcome='A benchmark that explains saturation and can be repeated across changes.', why='A fast isolated handler can still produce a slow product when database, network, serialization, or queue behavior dominates.', diagram='''flowchart LR\n    workload[Workload model] --> clients[Concurrent clients]\n    clients --> system[Complete topology]\n    system --> samples[Latency samples]\n    samples --> percentiles[p50 p95 p99]\n    percentiles --> decision[Capacity decision]''', steps=[('Model real work','Choose payloads, reads, writes, streams, tools, and dependency behavior.'),('Warm and cold run','Separate startup, cache, connection, and steady-state effects.'),('Increase pressure','Find the point where latency, errors, memory, or queues change slope.'),('Record context','Keep runtime, hardware, version, topology, and command with the result.')], tabs=[('Micro','Locate a function or serialization cost.'),('Load','Understand concurrent request behavior.'),('Soak','Find leaks, queue growth, and slow degradation.')], rows=[('p50','Typical request','Useful for baseline.'),('p95','Slow tail','Useful for user experience.'),('p99','Extreme tail','Useful for saturation and incidents.'),('RSS/heap','Memory behavior','Useful for leak and pressure detection.')], quote='A benchmark is a decision instrument, not a victory number.', note='Do not compare frameworks using different workloads, caches, connection pools, or failure policies.', accordions=[('What invalidates a result?','Unreported warmup, hidden cache, different topology, insufficient samples, or no error count.'),('Why track p99?','Tail latency often reveals queueing and dependency contention first.'),('What should a benchmark output?','Throughput, percentiles, errors, resource usage, and the tested configuration.')], next=[('/operations/production','Apply capacity findings','server-stack'),('/platform/health-observability','Instrument load','chart-line'),('/platform/sql','Test database pressure','database')]),
'operations/jobs-and-shutdown': dict(eyebrow='Operations · Jobs and shutdown', icon='power-off', lead='Make background work finish, retry, pause, or fail with a known outcome when the process changes state.', outcome='Idempotent jobs, signal handling, draining, retry policy, and bounded shutdown.', why='A process exit is normal infrastructure behavior; work should not depend on a lucky timing window.', diagram='''stateDiagram-v2\n    [*] --> Accepting\n    Accepting --> Draining: shutdown signal\n    Draining --> Completing: stop new work\n    Completing --> Stopped: queue empty or deadline\n    Draining --> Forced: deadline exceeded''', steps=[('Classify work','Separate safe retry, manual retry, and non-retryable failures.'),('Persist intent','Store enough state to resume or explain a job after restart.'),('Stop intake','Reject or defer new work when shutdown begins.'),('Drain with a deadline','Finish bounded work and record what was abandoned.')], tabs=[('Short job','May complete inside the request if cancellation is safe.'),('Durable job','Needs queue, status, retry, and idempotency.'),('Shutdown','Needs signal, drain, deadline, and forced-stop behavior.')], rows=[('Retry','Transient only','Do not duplicate irreversible effects.'),('Drain','Finite window','Bound deploy and restart time.'),('Abandon','Recorded','Make recovery visible and actionable.')], quote='Graceful shutdown is a correctness feature for any system that performs side effects.', note='Do not acknowledge a job before its durable intent and retry identity are recorded.', accordions=[('What does idempotent mean here?','Repeating the same delivery produces one effective outcome.'),('When force stop?','When the deadline is reached and continued work risks deploy or process health.'),('What should operators see?','Active count, oldest age, retry count, failed count, and drain phase.')], next=[('/platform/cache-jobs-storage','Choose job storage','layer-group'),('/core/execution-and-streaming','Cancel request work','stream'),('/operations/deployment','Deploy with drain','cloud-arrow-up')]),
'operations/release-checks': dict(eyebrow='Operations · Release checks', icon='circle-check', lead='Turn a source change into a release only after code, artifacts, compatibility, and provenance agree.', outcome='A release gate that protects users from broken types, missing files, and unsupported runtime behavior.', why='Publishing is the last step of a chain; it should not be the first place a package is tested as a consumer would use it.', diagram='''flowchart LR\n    change[Change] --> types[Typecheck]\n    types --> tests[Tests]\n    tests --> build[Build]\n    build --> pack[Pack inspection]\n    pack --> consumer[Consumer smoke test]\n    consumer --> publish[Publish]''', steps=[('Check source','Run formatting, typecheck, tests, and security checks.'),('Check artifact','Inspect files, exports, declarations, and package metadata.'),('Check consumer use','Install the tarball into a clean project and import public APIs.'),('Check provenance','Use the intended Node version, OIDC workflow, and release tag or trigger.')], tabs=[('Pull request','Fast feedback and no publication.'),('Release candidate','Full matrix and package consumer test.'),('Production publish','Protected environment and auditable workflow.')], rows=[('Source','Tests green','Behavior is acceptable.'),('Artifact','Files present','Package is usable.'),('Consumer','Import works','Public surface is real.'),('Publish','Trusted workflow','Provenance is controlled.')], quote='A release is ready when a clean consumer can use the artifact, not when the repository can build itself.', note='Keep publication credentials and trusted publishing policy outside application source.', accordions=[('Why inspect npm pack?','Build output and repository files are not the same package surface.'),('What does a smoke import catch?','Broken exports, missing declarations, and runtime module errors.'),('When publish?','Only after all matrix checks and the protected release workflow agree.')], next=[('/reference/compatibility','Review support matrix','scale-balanced'),('/reference/cli','Automate checks','terminal'),('/operations/deployment','Deploy the artifact','cloud-arrow-up')]),
'operations/ecosystem': dict(eyebrow='Operations · Ecosystem', icon='sitemap', lead='Scale adoption through quality, discoverability, compatibility policy, and maintainership—not through a long provider list.', outcome='A sustainable ecosystem contract for contributors, integration authors, and operators.', why='Every integration creates support surface; quality metadata lets users choose safely and maintainers prioritize responsibly.', diagram='''flowchart LR\n    contributor[Contributor] --> contract[Integration contract]\n    contract --> tests[Compatibility tests]\n    tests --> catalog[Catalog and docs]\n    catalog --> users[Users and feedback]\n    users --> maintainer[Maintenance decision]''', steps=[('Publish the contract','Define capability, lifecycle, security, versioning, and support expectations.'),('Require evidence','Add tests, examples, failure behavior, and compatibility metadata.'),('Make discovery useful','Organize integrations by real capability and maturity.'),('Close feedback loops','Track issues, deprecations, ownership, and migration paths.')], tabs=[('Author','Build and document one high-quality integration.'),('Consumer','Compare capability, maturity, and operational cost.'),('Maintainer','Review compatibility, security, and support load.')], rows=[('Discovery','Clear metadata','Users find the right provider.'),('Quality','Tests and examples','Users can trust the integration.'),('Governance','Owner and lifecycle','The ecosystem can evolve.')], quote='An ecosystem is infrastructure for other developers; its documentation is part of its API.', note='Avoid vanity counts. A smaller catalog with clear guarantees is more useful than an unmaintained directory.', accordions=[('What earns a stable label?','Compatibility evidence, security review, documentation, and an accountable maintainer.'),('How handle deprecation?','Announce, document replacement, provide a window, and preserve migration context.'),('What should contributors avoid?','Provider-specific assumptions leaking into the framework core.')], next=[('/reference/integrations','Register capabilities','plug'),('/reference/adapters','Implement boundaries','puzzle-piece'),('/operations/release-checks','Protect releases','circle-check')]),
}



def table(rows):
    return '\n'.join(f'| {a} | {b} | {c} |' for a, b, c in rows)


def related(links):
    return '\n'.join(f'- [{icon} · **{title}**]({href}) — Read the focused guide for this boundary.' for href, title, icon in links)


def roadmap(d):
    return '\n'.join(f'  <Step title="{title}" icon="{["download", "route", "database", "rocket"][i]}">\n    {body}\n  </Step>' for i, (title, body) in enumerate(d['steps']))


def concept_cards(d):
    icons = ["sparkles", "shield-check", "gauge-high"]
    return "\n".join(
        f'  <Card title="{title}" icon="{icons[i]}">\n    {body}\n  </Card>'
        for i, (title, body, _) in enumerate(
            [(a, b, c) for a, b, c in d['rows'][:3]]
        )
    )


def decision_lens(d):
    return "\n".join(f'| **{title}** | {body} |' for title, body in d['tabs'])


def implementation_notes(d):
    return "\n".join(f'| **{title}** | {body} |' for title, body in d['steps'])


def render(rel: str, d: dict) -> str:
    links = related(d['next'])
    if rel == 'index':
        body = f'''\n\n---\n\n## {d['eyebrow']}\n\n> **A practical map** — {d['lead']}\n\n{d['outcome']}\n\n### Key concepts\n\n<Columns cols={{3}}>\n{concept_cards(d)}\n</Columns>\n\n### Jeston from first install to production\n\n```mermaid placement="top-right"\n{d['diagram']}\n```\n\n<Steps titleSize="h3">\n{roadmap(d)}\n</Steps>\n\n## Choose a track\n\n| Track | Start here | What it proves |\n| --- | --- | --- |\n{table(d['rows'])}\n\n<Info>\nThe roadmap is intentionally kept on this page. The guides below are reference material for a specific engineering decision, not another onboarding sequence.\n</Info>\n\n## Related topics\n\n<Columns cols={{3}}>\n{links}\n</Columns>\n'''
    elif rel.startswith('start/'):
        body = f'''\n\n---\n\n## {d['eyebrow']}\n\n> **Purpose** — {d['lead']}\n\n{d['outcome']}\n\n### Key concepts\n\n<Columns cols={{3}}>\n{concept_cards(d)}\n</Columns>\n\n### What to verify\n\n| Area | Expected state | Evidence |\n| --- | --- | --- |\n{table(d['rows'])}\n\n### Working example\n\n```text\nrequest -> Jeston runtime -> typed boundary -> observable response\n```\n\n### Before you move on\n\n{d['why']}\n\n### Implementation notes\n\n| Engineering move | Guidance |\n| --- | --- |\n{implementation_notes(d)}\n\n### Decision lens\n\n| Mode | Practical emphasis |\n| --- | --- |\n{decision_lens(d)}\n\n## Related topics\n\n<Columns cols={{3}}>\n{links}\n</Columns>\n'''
    elif rel.startswith('core/'):
        body = f'''\n\n---\n\n## {d['eyebrow']}\n\n> **Core principle** — {d['lead']}\n\n{d['outcome']}\n\n### Key concepts\n\n<Columns cols={{3}}>\n{concept_cards(d)}\n</Columns>\n\n### Boundary model\n\n```mermaid placement="top-right"\n{d['diagram']}\n```\n\n### Ownership matrix\n\n| Concern | Jeston/application boundary | Review signal |\n| --- | --- | --- |\n{table(d['rows'])}\n\n### Engineering considerations\n\n{d['why']}\n\n> **Design constraint**\n>\n> {d['quote']}\n\n### Implementation notes\n\n| Engineering move | Guidance |\n| --- | --- |\n{implementation_notes(d)}\n\n### Decision lens\n\n| Mode | Practical emphasis |\n| --- | --- |\n{decision_lens(d)}\n\n## Related topics\n\n<Columns cols={{3}}>\n{links}\n</Columns>\n'''
    elif rel.startswith('platform/'):
        body = f'''\n\n---\n\n## {d['eyebrow']}\n\n> **Platform concern** — {d['lead']}\n\n{d['outcome']}\n\n### Key concepts\n\n<Columns cols={{3}}>\n{concept_cards(d)}\n</Columns>\n\n### Operating model\n\n```mermaid placement="top-right"\n{d['diagram']}\n```\n\n### Decisions to make before production\n\n| Decision | Recommended posture | Failure it prevents |\n| --- | --- | --- |\n{table(d['rows'])}\n\n### Boundary and failure behavior\n\n{d['why']}\n\n### Questions for review\n\n| Question | Answer to document |\n| --- | --- |\n{chr(10).join(f'| {title} | {body} |' for title, body in d['accordions'])}\n\n### Implementation notes\n\n| Engineering move | Guidance |\n| --- | --- |\n{implementation_notes(d)}\n\n### Decision lens\n\n| Mode | Practical emphasis |\n| --- | --- |\n{decision_lens(d)}\n\n## Related topics\n\n<Columns cols={{3}}>\n{links}\n</Columns>\n'''
        if rel in {'platform/security', 'platform/ai-agents'}:
            body = body.replace('\n### Boundary and failure behavior\n', '\n<Note>\n' + d['note'] + '\n</Note>\n\n### Boundary and failure behavior\n', 1)
    elif rel.startswith('operations/'):
        operational_heading = 'Runbook view' if rel == 'operations/production' else ('Evidence model' if rel in {'operations/benchmark', 'operations/health-observability'} else 'Operational surface')
        body = f'''\n\n---\n\n## {d['eyebrow']}\n\n> **Operational objective** — {d['lead']}\n\n{d['outcome']}\n\n### Key concepts\n\n<Columns cols={{3}}>\n{concept_cards(d)}\n</Columns>\n\n### {operational_heading}\n\n```mermaid placement="top-right"\n{d['diagram']}\n```\n\n### Evidence over assumption\n\n| Signal | Healthy interpretation | Action when it degrades |\n| --- | --- | --- |\n{table(d['rows'])}\n\n### Operational context\n\n{d['why']}\n\n> **Operator's principle**\n>\n> {d['quote']}\n\n### Implementation notes\n\n| Engineering move | Guidance |\n| --- | --- |\n{implementation_notes(d)}\n\n### Decision lens\n\n| Mode | Practical emphasis |\n| --- | --- |\n{decision_lens(d)}\n\n## Related topics\n\n<Columns cols={{3}}>\n{links}\n</Columns>\n'''
        if rel in {'operations/production', 'operations/benchmark', 'operations/migrations'}:
            body = body.replace('\n### Operational context\n', '\n<Warning>\n' + d['note'] + '\n</Warning>\n\n### Operational context\n', 1)
    else:
        body = f'''\n\n---\n\n## {d['eyebrow']}\n\n> **Reference purpose** — {d['lead']}\n\n{d['outcome']}\n\n### Key concepts\n\n<Columns cols={{3}}>\n{concept_cards(d)}\n</Columns>\n\n### Reference model\n\n```mermaid placement="top-right"\n{d['diagram']}\n```\n\n### Contract summary\n\n| Property | Definition | Review question |\n| --- | --- | --- |\n{table(d['rows'])}\n\n### Interpretation\n\n{d['why']}\n\n> **Reference note**\n>\n> {d['quote']}\n\n### Implementation notes\n\n| Engineering move | Guidance |\n| --- | --- |\n{implementation_notes(d)}\n\n### Decision lens\n\n| Mode | Practical emphasis |\n| --- | --- |\n{decision_lens(d)}\n\n## Related topics\n\n<Columns cols={{3}}>\n{links}\n</Columns>\n'''
    return body


for path in sorted(ROOT.rglob('*.mdx')):
    rel = path.relative_to(ROOT).with_suffix('').as_posix()
    if rel == 'untitled-page' or rel not in PAGES:
        continue
    source = path.read_text()
    parts = source.split('---', 2)
    frontmatter = parts[1]
    remainder = parts[2] if len(parts) > 2 else source
    heading = next((line for line in remainder.splitlines() if line.startswith('# ')), f'# {PAGES[rel]["eyebrow"]}')
    references = re.findall(r'^\[\d+\]:.*$', source, flags=re.MULTILINE)
    refs = '\n## References\n\n' + '\n'.join(references) + '\n' if references else ''
    path.write_text(f'---{frontmatter}---\n\n{heading}' + render(rel, PAGES[rel]) + refs)

print(f'Rebuilt {len(PAGES)} pages with editorial layouts')
