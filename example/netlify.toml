[build]
  command = "npm run export"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# O app é exportado como HTML estático para a CDN da Netlify.
# Para manter SSR e APIs, use o pacote Node gerado por `npm run deploy` em uma plataforma com servidor.
