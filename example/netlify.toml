[build]
  command = "npm run export"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# The app is exported as static HTML for the Netlify CDN.
# To preserve SSR and APIs, use the Node package generated by `npm run deploy` on a platform with a server runtime.
